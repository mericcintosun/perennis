// Tombstone. The module that lived in this file was renamed by function in
// Phase 8 and now lives in components/address-copy.tsx. The file is left empty
// only because the agent that renamed it has no shell and cannot run `git rm`.
// Delete the file.
export {};
