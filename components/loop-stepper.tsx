// Tombstone. The module that lived in this file was removed in Phase 8 and
// nothing imports it any more. The file is left empty only because the agent
// that removed it has no shell and cannot run `git rm`. Delete the file.
export {};
