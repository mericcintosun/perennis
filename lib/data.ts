// TEMPLATE: replace with 8-20 plausible, domain-specific seed records with a
// real TypeScript type. Fixed ids, realistic values; never foo/bar/item 1.
export interface SeedItem {
  id: string;
  name: string;
}

export const seed: SeedItem[] = [
  { id: "t-1", name: "Template seed row (replace me)" },
];
